public enum TaskStatus{
    RECEIVED,
    IN_REPAIR,
    REPAIRED,
    NOT_REPAIRED,
    WAITING_FOR_CLIENT,
    COLLECTED
}

public enum RoleType
{
    ADMIN,
    MANAGER,
    EMPLOYEE,
}

public enum TaskDetailStatus
{
    DURING,
    DONE,
    NOT_DONE
}